description = 'Spek Implementation'

dependencies {
    compile project(':spek-api')
    compile group: 'org.reflections', name: 'reflections', version: '0.9.8'
}

sourceSets {
    main.java.srcDirs += 'java'
    main.kotlin.srcDirs += 'kotlin'
    main.kotlin.srcDirs += 'test/kotlin'
}