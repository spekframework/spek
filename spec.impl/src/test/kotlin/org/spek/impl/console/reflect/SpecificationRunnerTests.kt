package org.spek.impl.console.reflect

public class SpecificationRunnerTests {
    /*

        test fun creating_a_specification_runner_should_return_instance() {
            val mockListener = mock(javaClass<Listener>())

            val runner = SpecificationRunner(mockListener!!)

            assertNotNull(runner)
        }

        test fun a_specification_run_with_0_specifications_should_notify_of_RunStarted_with_0_specifications_and_notify_of_RunFinished_with_all_0s() {
            val mockListener = mock(javaClass<Listener>())
            val runner = SpecificationRunner(mockListener!!)

            runner.runSpecs("","")

            verify(mockListener)?.notify(RunStarted(0))
            verify(mockListener)?.notify(RunFinished(0,0,0,0))
        }

        test fun a_specification_run_with_more_than_0_specifications_should_notify_of_RunStarted_and_RunFinished_with_correct_specifications() {
            val mockListener = mock(javaClass<Listener>())
            val runner = SpecificationRunner(mockListener!!)

            runner.runSpecs("out/test/spek/org/spek/test/samples", "org.spek.test.samples")

            verify(mockListener)?.notify(RunStarted(2))
            verify(mockListener)?.notify(RunFinished(3,0,0,0))
        }



    */

}

